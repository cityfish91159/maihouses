# MaiHouses Cline Rules v2.0 - 強制工具搜尋版

## 🎯 專案資訊
- **名稱**: MaiHouses (邁房子)
- **技術棧**: React 18 + TypeScript + Vite + Tailwind CSS
- **後端**: Vercel Serverless Functions + Supabase
- **首頁**: https://maihouses.vercel.app/maihouses/

---

## 🚨🚨🚨 第零條天條：強制 Skills 搜尋 🚨🚨🚨

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║   ⛔ 開始任何任務前，必須先使用 SkillsMP 搜尋適合的工具 ⛔                     ║
║                                                                              ║
║   步驟：                                                                      ║
║   1. 使用 search_skills 或 ai_search_skills 搜尋工具                          ║
║   2. 在任務報告中列出找到的 Skills                                            ║
║   3. 優先使用已有的 Skills，不重複造輪子                                       ║
║                                                                              ║
║   沒有搜尋工具就開始寫代碼 = 違規 = 任務失敗                                   ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## 🚨 核心原則

### 絕對禁止
1. **禁止跳過 Skills 搜尋** - 必須先搜尋工具再開始任務
2. **禁止 `any` 類型** - 必須使用明確的 TypeScript 類型
3. **禁止跳過步驟** - 必須按順序完成每個步驟
4. **禁止便宜行事** - 不可用簡單但錯誤的方案敷衍
5. **禁止腦補需求** - 不清楚就問，不可假設用戶意圖
6. **禁止硬編碼密鑰** - 所有密鑰必須用環境變數
7. **禁止盲寫代碼** - 修改前必須先讀取檔案

### 絕對必須
1. **先查 Skills** - 開始任務前必須先用 SkillsMP 搜尋合適的工具
2. **先讀後寫** - 修改任何檔案前必須先讀取該檔案
3. **讀對檔案** - 必須讀取所有相關的前端、後端、類型定義檔案
4. **先計畫後執行** - 列出所有步驟再動手
5. **完整錯誤處理** - 每個 async/await 都要有 try/catch
6. **繁體中文** - UI 文字使用繁體中文（台灣用語）
7. **完整測試** - 完成後必須執行 typecheck/lint/build

---

## 🔧 Skills 使用規範

### 開始任務前必做
1. **搜尋相關 Skills** - 使用 SkillsMP 搜尋與任務相關的工具
2. **評估適用性** - 判斷哪些 Skills 可以幫助完成任務
3. **優先使用 Skills** - 有合適的工具就用，不要重複造輪子
4. **記錄使用工具** - 在報告中列出使用了哪些 Skills

### Skills 搜尋關鍵字對照表

| 任務類型 | 必搜關鍵字 | 額外關鍵字 |
|---------|-----------|-----------|
| 前端 UI | `react`, `component` | `tailwind`, `ui`, `form` |
| API 開發 | `api`, `rest` | `fetch`, `serverless`, `endpoint` |
| 資料庫 | `database`, `supabase` | `postgres`, `sql`, `query` |
| 認證 | `auth`, `authentication` | `jwt`, `oauth`, `session` |
| 測試 | `test`, `testing` | `jest`, `playwright`, `e2e` |
| 部署 | `deploy`, `vercel` | `ci/cd`, `github-actions` |
| AI/LLM | `openai`, `claude` | `llm`, `chat`, `embedding` |
| 圖片處理 | `image`, `upload` | `imgix`, `compression` |

---

## 📋 任務執行協議

### 收到任務時
```
╔══════════════════════════════════════════════════════════════╗
║ 📋 任務接收                                                  ║
╠══════════════════════════════════════════════════════════════╣
║ 🎯 任務：[用一句話描述任務]                                   ║
║ ⏰ 開始：[當前時間]                                          ║
╠══════════════════════════════════════════════════════════════╣
║ 🔍 Skills 搜尋結果：                                          ║
║   • 搜尋關鍵字：[關鍵字1], [關鍵字2]                          ║
║   • 找到工具：[工具1] - [用途]                                ║
║   • 決定使用：[選擇的工具]                                    ║
╠══════════════════════════════════════════════════════════════╣
║ 📖 已閱讀檔案：                                               ║
║   • [file1.ts] - [原因]                                       ║
╠══════════════════════════════════════════════════════════════╣
║ 📝 工作清單：                                                ║
║   ⬜ 1. [第一個子任務]                                        ║
║   ⬜ 2. [第二個子任務]                                        ║
║   ⬜ 3. [第三個子任務]                                        ║
╚══════════════════════════════════════════════════════════════╝
```

### 完成子任務時
```
═══════════════════════════════════════════════════════════════
📊 進度報告 [X/Y]
═══════════════════════════════════════════════════════════════
✅ 剛完成：[完成的項目描述]
📁 修改的檔案：[file.ts] - [改了什麼]
🔧 使用的工具：[使用了哪些 Skills]

📝 清單狀態：
  ✅ 1. [已完成項目]
  🔧 2. [當前進行中] ← 現在
  ⬜ 3. [待處理項目]
═══════════════════════════════════════════════════════════════
```

### 任務完成時
```
╔══════════════════════════════════════════════════════════════╗
║ 🏁 任務完成                                                  ║
╠══════════════════════════════════════════════════════════════╣
║ 🎯 任務：[任務描述]                                          ║
╠══════════════════════════════════════════════════════════════╣
║ ✅ 完成清單：                                                 ║
║   ✅ 1. [完成項目]                                            ║
║   ✅ 2. [完成項目]                                            ║
╠══════════════════════════════════════════════════════════════╣
║ 📁 修改的檔案：                                               ║
║   • [file1.ts] - [改了什麼]                                   ║
║ 📦 使用的 Skills：                                            ║
║   • [skill-name] - [用途]                                     ║
╠══════════════════════════════════════════════════════════════╣
║ ✅ 測試結果：                                                  ║
║   • npm run typecheck ✓                                       ║
║   • npm run lint ✓                                            ║
║   • npm run build ✓                                           ║
╚══════════════════════════════════════════════════════════════╝
```

---

## 📁 重要檔案位置

| 目的 | 路徑 |
|------|------|
| 專案規範 | `CLAUDE.md` |
| AI 天條 | `.github/copilot-instructions.md` |
| Cline 規則 | `.cline/rules/global.md` |
| TODO 追蹤 | `docs/COMMUNITY_WALL_TODO.md` |
| 環境變數範例 | `.env.example` |
| API 端點 | `api/` |
| 前端組件 | `src/components/` |
| 頁面 | `src/pages/` |
| 服務層 | `src/services/` |
| 類型定義 | `src/types/` |
| MaiMai 吉祥物 | `src/components/MaiMai/` |
| MCP 伺服器 | `mcp-servers/` |

---

## 🔧 代碼規範

### TypeScript
```typescript
// ✅ 正確 - 完整類型定義
interface Property {
  id: string;
  title: string;
  price: number;
}

// ✅ 正確 - 完整錯誤處理
const fetchProperty = async (id: string): Promise<Property> => {
  try {
    const response = await fetch(`/api/property/${id}`);
    if (!response.ok) {
      throw new Error(`HTTP ${response.status}`);
    }
    return response.json();
  } catch (error) {
    const message = error instanceof Error ? error.message : '未知錯誤';
    console.error('Fetch error:', message);
    throw error;
  }
};

// ❌ 禁止 - any 類型
const data: any = fetchData();

// ❌ 禁止 - 空的 catch
try { ... } catch (e) { /* 空的 */ }
```

### React 組件
```tsx
// ✅ 正確 - 使用 function 組件 + TypeScript
interface Props {
  title: string;
  onClose: () => void;
}

export function MyComponent({ title, onClose }: Props) {
  return <div onClick={onClose}>{title}</div>;
}

// ❌ 禁止 - 使用 class 組件或 any
```

### Tailwind CSS
```tsx
// ✅ 正確 - 使用 Tailwind 類名
<div className="flex items-center gap-4 p-4 bg-white rounded-lg shadow-md">

// ❌ 禁止 - 使用內聯樣式
<div style={{ display: 'flex', alignItems: 'center' }}>
```

---

## 🌐 語言規範

- **UI 文字**: 繁體中文（台灣用語）
- **變數/函數名**: 英文 camelCase
- **類型/介面名**: 英文 PascalCase
- **錯誤訊息**: 繁體中文
- **註解**: 繁體中文或英文皆可

---

## 🧪 驗證命令

完成修改後**必須**執行：
```bash
npm run typecheck    # TypeScript 類型檢查
npm run lint         # ESLint 檢查
npm run build        # 構建檢查
```

**任何一項失敗 = 任務未完成**

---

## 📝 提交規範

```bash
# 功能
git commit -m "feat: 新增 XXX 功能"

# 修復
git commit -m "fix: 修復 XXX 問題"

# 重構
git commit -m "refactor: 重構 XXX 模組"

# 樣式
git commit -m "style: 調整 XXX 樣式"

# 文件
git commit -m "docs: 更新 XXX 文件"
```

---

## ⚠️ 環境變數

必須在 Vercel Dashboard 設定：
- `OPENAI_API_KEY` - OpenAI API
- `ANTHROPIC_API_KEY` - Claude API
- `REPLICATE_API_TOKEN` - Replicate AI
- `SUPABASE_URL` - Supabase URL
- `SUPABASE_SERVICE_ROLE_KEY` - Supabase Key

**絕對禁止**在代碼中硬編碼任何密鑰！

---

## 🎭 MaiMai 吉祥物開發

參考 `docs/COMMUNITY_WALL_TODO.md` 的 MM-1 ~ MM-5 任務。

心情類型：
```typescript
type MaiMaiMood = 
  | 'idle' | 'wave' | 'thinking' | 'happy' 
  | 'excited' | 'confused' | 'celebrate' 
  | 'shy' | 'sleep' | 'peek';
```

---

*版本：v2.0 強制工具搜尋版*
*最後更新：2025-12-24*