# ⛔️ maihouses AI 強制規範 (Cursor IDE) ⛔️

## 🚨 最高優先級 - 任何 AI 必須遵守

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║   🚨🚨🚨 在這個專案中，AI 必須遵守以下強制規範 🚨🚨🚨                         ║
║                                                                              ║
║   📖 完整規範: 專案根目錄 CLAUDE.md                                           ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
```

---

## ❌ 絕對禁止 (NEVER DO)

| 禁止項目 | 原因 | 替代方案 |
|----------|------|----------|
| `: any` | 失去類型安全 | 定義具體 interface/type |
| `console.log` | 生產環境不應有 | 使用正式 logger 或移除 |
| `// @ts-ignore` | 隱藏類型錯誤 | 修復根本問題 |
| `// eslint-disable` | 隱藏代碼問題 | 修復根本問題 |
| 硬編碼字串/數字 | 難以維護 | 使用常數或配置 |
| 超過 50 行的函數 | 難以測試維護 | 拆分成小函數 |
| 未讀取就修改 | 會破壞現有代碼 | 先 Read 再 Edit |

---

## ✅ 絕對必須 (ALWAYS DO)

### 1. 先讀後寫 (Read Before Edit)

```
📖 READ → 🧠 UNDERSTAND → ✏️ EDIT

修改任何檔案前，必須先閱讀：
- 要修改的檔案本身
- 該檔案 import 的所有模組
- 相關的類型定義檔案 (types.ts, *.d.ts)
- 相關的 API/服務層檔案
- 相關的 hooks 和 context
```

### 2. 類型安全

```typescript
// ❌ 禁止
const data: any = fetchData();
const handleClick = (e: any) => {};

// ✅ 正確
interface UserData {
  id: string;
  name: string;
  email: string;
}
const data: UserData = fetchData();
const handleClick = (e: React.MouseEvent<HTMLButtonElement>) => {};
```

### 3. 完整錯誤處理

```typescript
// ❌ 禁止
const fetchUser = async (id: string) => {
  const res = await fetch(`/api/user/${id}`);
  return res.json();
};

// ✅ 正確
const fetchUser = async (id: string): Promise<User> => {
  try {
    const res = await fetch(`/api/user/${id}`);
    if (!res.ok) {
      throw new Error(`Failed to fetch user: ${res.status}`);
    }
    return res.json();
  } catch (error) {
    console.error('fetchUser error:', error);
    throw error;
  }
};
```

### 4. Zod Schema 類型 (本專案使用)

```typescript
import { z } from 'zod';
import type { FeaturedPropertyCard } from '../types/property-page';

// 使用專案已定義的 Zod Schema 類型
// 不要自己創建重複的 interface
```

---

## 📋 任務執行流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 理解需求 - 完整閱讀，不清楚就問                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 識別相關檔案 - 列出所有需要閱讀和修改的檔案               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 完整閱讀 - 先讀後寫，理解現有架構                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. 規劃修改 - 列出具體步驟                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. 逐步執行 - 一次一個檔案，每步驗證                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. 驗證結果                                                  │
│    - npm run typecheck (類型檢查)                            │
│    - npm run lint (代碼風格)                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏗️ 專案技術架構

- **前端**: React 18 + TypeScript + Vite
- **樣式**: Tailwind CSS
- **後端**: Vercel Serverless Functions
- **資料庫**: Supabase (PostgreSQL)
- **部署**: Vercel
- **類型系統**: Zod Schema

---

## 📁 重要檔案位置

```
maihouses/
├── CLAUDE.md                 # 📖 完整 AI 規範 (必讀)
├── src/
│   ├── types/               # TypeScript 類型定義
│   │   └── property-page.ts # Zod Schema 類型
│   ├── pages/               # 頁面組件
│   ├── components/          # UI 組件
│   ├── features/            # 功能模組
│   └── lib/                 # 工具函數
├── api/                     # Vercel Serverless API
└── .github/
    └── copilot-instructions.md # Copilot 規範
```

---

## 🌐 語言規範

- **UI 文字**: 繁體中文
- **代碼/變數**: 英文
- **錯誤訊息**: 繁體中文
- **註解**: 可用中文或英文

---

## 🔧 驗證命令

```bash
# TypeScript 類型檢查
npm run typecheck

# ESLint 代碼風格
npm run lint

# 構建檢查
npm run build
```

---

## ⚠️ 違規即失敗

任何違反以上規範的代碼將被拒絕。沒有例外。

**記住：寧可多花時間做對，也不要圖快做錯。品質永遠優先於速度。**

---

*版本：v1.0*
*適用於：Cursor IDE*
*完整規範：CLAUDE.md*
