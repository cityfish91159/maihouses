# ===== AI 連線設定（擇一）=====
# 1) 直接使用 OpenAI API Key（不建議在前端正式環境暴露）
VITE_OPENAI_API_KEY=sk-proj-your-api-key-here

# 2) 使用代理（Cloudflare Workers / 其他後端）
# 若設定此值且同時存在 KEY，優先使用 KEY 直連；只有在 KEY 缺失時才回退到代理
VITE_AI_PROXY_URL=https://mai-ai-proxy.example.workers.dev/api/chat

# ===== Replicate API Token (for image generation) =====
REPLICATE_API_TOKEN=

# ===== Replicate Deployment (format: owner/deployment-name) =====
# Example: cityfish91159/maihouses-flux (for image generation)
REPLICATE_DEPLOYMENT=

# Example: cityfish91159/maihouses-detect (for object detection)
REPLICATE_DEPLOYMENT_DETECT=

# ===== X-Ray Mike Model (for X-ray image processing) =====
# Model path: cityfish91159/x-raymike
# No deployment needed - uses direct model API
REPLICATE_XRAY_MODEL=cityfish91159/x-raymike

# ===== 使用說明 =====
# 將本檔複製為 .env 或 .env.local 並填入真實值。
# 前端呼叫順序：
#   a. 有 VITE_OPENAI_API_KEY -> https://api.openai.com/v1/chat/completions
#   b. 無金鑰但有 VITE_AI_PROXY_URL -> 該代理 URL
#   c. 否則 -> /api/chat（GitHub Pages 會失敗，請務必至少設定代理）
