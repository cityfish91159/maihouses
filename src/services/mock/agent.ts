/**
 * Agent Mock Data Backward-Compatible Exports
 *
 * Source of truth moved to `src/constants/mockData.ts`.
 * Keep this module to avoid breaking existing imports.
 */

export { MOCK_UAG_SUMMARY, MOCK_PERFORMANCE_STATS, MOCK_TODO_LIST } from '../../constants/mockData';
