/**
 * Feed Mock Data Backward-Compatible Exports
 *
 * Source of truth moved to `src/constants/mockData.ts`.
 * Keep this module to avoid breaking existing imports.
 */

export {
  MOCK_FEED_STATS,
  MOCK_SALE_ITEMS,
  MOCK_ACTIVE_TRANSACTION,
} from '../../constants/mockData';
