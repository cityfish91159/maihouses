name: 系統重構自動化
on:
  schedule:
    - cron: '0 17 * * *'  # UTC 17:00 = 台北時間凌晨 1:00
  workflow_dispatch:       # 也可手動觸發

jobs:
  refactor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout 程式碼
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 設定 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 安裝依賴
        run: npm ci

      - name: 執行系統重構
        run: |
          chmod +x scripts/refactor-structure.sh
          ./scripts/refactor-structure.sh

      - name: 測試建置
        run: npm run build

      - name: 提交變更
        run: |
          git config user.name "Maihouses Bot"
          git config user.email "bot@maihouses.dev"
          git add .
          
          if git diff --staged --quiet; then
            echo "✅ 無需重構，結構已符合規範"
            exit 0
          fi
          
          git commit -m "chore: 自動系統重構 - 符合規格書架構

          🤖 此為自動化重構
          📅 執行時間: $(date +'%Y-%m-%d %H:%M:%S')
          📋 變更內容:
          - 重組型別檔案位置
          - 調整模組目錄結構
          - 確保離散式架構規範
          
          Refs: docs/GUIDELINES.md §4 離散式架構"
          
          git push

      - name: 發送通知（如果有變更）
        if: success()
        run: |
          echo "🎉 系統重構完成並已推送"
          echo "查看變更: https://github.com/${{ github.repository }}/commit/$(git rev-parse HEAD)"
